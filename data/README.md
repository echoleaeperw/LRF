# 数据目录

此目录用于存放项目所需的数据集和生成的场景。

## 📁 目录结构

```
data/
├── nuscenes/        # nuScenes数据集（需下载）
├── clustering/      # 聚类模型和标签
└── scenarios/       # 生成的场景数据
```

## 📥 nuScenes数据集

### 下载地址
https://www.nuscenes.org/download

### 需要下载的文件

#### 最小数据集（用于测试）
- **v1.0-mini** (~4GB)
  - 包含10个场景
  - 适合快速测试和开发

#### 完整数据集（用于训练）
- **v1.0-trainval** (~248GB)
  - 包含850个场景
  - 用于完整模型训练

#### 地图数据
- **nuScenes-map-expansion-v1.3** (~60MB)
  - 包含详细的地图信息
  - 必需组件

### 下载后的操作

1. 解压下载的文件
2. 将内容放入 `nuscenes/` 目录
3. 确保目录结构如下：

```
data/nuscenes/
├── maps/
├── samples/
├── sweeps/
├── v1.0-mini/  (或 v1.0-trainval/)
└── ...
```

### 验证安装

```python
from nuscenes.nuscenes import NuScenes

# 验证mini数据集
nusc = NuScenes(version='v1.0-mini', dataroot='./data/nuscenes', verbose=True)
print(f"Loaded {len(nusc.scene)} scenes")
```

## 📊 聚类数据

`clustering/` 目录包含场景聚类的结果：
- `cluster_labels.txt` - 聚类标签
- `cluster.pkl` - 聚类模型（需训练生成）

## 🎬 生成的场景

`scenarios/` 目录用于存放生成的对抗场景：
- 由 `src/adv_scenario_gen.py` 生成
- 包含轨迹、状态和元数据

## 💾 磁盘空间需求

- **最小配置**: ~5GB (v1.0-mini)
- **完整配置**: ~250GB (v1.0-trainval)
- **生成数据**: ~1-10GB (取决于生成场景数量)

## ⚠️ 注意事项

1. nuScenes数据集受版权保护，仅供研究使用
2. 下载需要注册账号
3. 建议使用高速网络下载
4. 确保有足够的磁盘空间

## 📄 许可证

nuScenes数据集使用需遵守其[许可协议](https://www.nuscenes.org/terms-of-use)。

